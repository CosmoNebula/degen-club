# ML stack for degen-club Phase 2+
# Python 3.14 compatible. CPU-only, no GPU.
# Primary algorithm: scikit-learn's HistGradientBoostingClassifier
# (LightGBM/XGBoost equivalent without macOS libomp dependency)

# Core ML / data
numpy>=1.26
pandas>=2.0
scipy>=1.10
scikit-learn>=1.4

# Inference service
fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.0

# Persistence + utility
joblib>=1.3
python-dotenv>=1.0

# DB driver: sqlite3 is in the standard library, no install needed.

# Optional (not currently used — would require `brew install libomp` on macOS):
# lightgbm>=4.0
# xgboost>=2.0
